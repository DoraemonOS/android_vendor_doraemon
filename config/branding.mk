# Set all versions
CUSTOM_BUILD_TYPE ?= Alpha

CUSTOM_DATE_YEAR := $(shell date -u +%Y)
CUSTOM_DATE_MONTH := $(shell date -u +%m)
CUSTOM_DATE_DAY := $(shell date -u +%d)
CUSTOM_DATE_HOUR := $(shell date -u +%H)
CUSTOM_DATE_MINUTE := $(shell date -u +%M)
CUSTOM_BUILD_DATE_UTC := $(shell date -d '$(CUSTOM_DATE_YEAR)-$(CUSTOM_DATE_MONTH)-$(CUSTOM_DATE_DAY) $(CUSTOM_DATE_HOUR):$(CUSTOM_DATE_MINUTE) UTC' +%s)
CUSTOM_BUILD_DATE := $(CUSTOM_DATE_YEAR)$(CUSTOM_DATE_MONTH)$(CUSTOM_DATE_DAY)-$(CUSTOM_DATE_HOUR)$(CUSTOM_DATE_MINUTE)

CUSTOM_PLATFORM_VERSION := Quiche

CUSTOM_DORAEMON_VERSION := v0.2

TARGET_PRODUCT_SHORT := $(subst doraemon_,,$(CUSTOM_BUILD))

CUSTOM_VERSION := DoraemonOS_$(CUSTOM_BUILD)-$(CUSTOM_DORAEMON_VERSION)-$(CUSTOM_PLATFORM_VERSION)-$(CUSTOM_BUILD_TYPE)
CUSTOM_VERSION_PROP := 10
ROM_FINGERPRINT := DoraemonOS/$(CUSTOM_PLATFORM_VERSION)/$(TARGET_PRODUCT_SHORT)/$(CUSTOM_BUILD_DATE)

CUSTOM_PROPERTIES := \
    ro.doraemon.version=$(CUSTOM_VERSION_PROP) \
    ro.doraemon.version.display=$(CUSTOM_VERSION) \
    ro.doraemon.build_date=$(CUSTOM_BUILD_DATE) \
    ro.doraemon.build_date_utc=$(CUSTOM_BUILD_DATE_UTC) \
    ro.doraemon.build_type=$(CUSTOM_BUILD_TYPE) \
    ro.doraemon.fingerprint=$(ROM_FINGERPRINT)

ifeq ($(CUSTOM_BUILD_TYPE), OFFICIAL)
PRODUCT_DEFAULT_DEV_CERTIFICATE := vendor/secure/releasekey
endif
